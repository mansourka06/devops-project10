---
# defaults vars file for jenkins

prerequisites_packages:
  - curl
  - openssh-server
  - ca-certificates
  - fontconfig

openjdk_version: openjdk-17-jre

jenkins_host: "{{ ansible_default_ipv4.address }}"
jenkins_port: 8080